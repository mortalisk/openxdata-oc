package org.openxdata.server.admin.model.state;

/**
 * Models the Editable's possible state transitions. 
 *
 */
public enum EditableState {
	
	/** State for an Editable that was successfully loaded.*/
	LOADED,
	
	/** State for an Editable that has data. */
	HASDATA

}
